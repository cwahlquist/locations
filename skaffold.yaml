apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/cwahlquist/locations:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/cwahlquist/locations:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: locations
        chartPath: charts/locations
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/cwahlquist/locations'
          image.tag: '{{.DIGEST_HEX}}'
